import { Direction } from "./components";

export const CHARACTERS = ["@", "#", "."];
export const INPUT_DIRECTION = {
  w: Direction.Up,
  s: Direction.Down,
  a: Direction.Left,
  d: Direction.Right
};
